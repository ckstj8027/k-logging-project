// Out-Adapter: JPA 영속성 어댑터. infrastructure 의 조회/저장 포트를 구현한다.
plugins {
    id 'cnapp.java-boot-jpa'
}

dependencies {
    implementation project(':msa-version:query:model')
    implementation project(':msa-version:query:infrastructure')
}
