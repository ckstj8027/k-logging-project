# [K-Sentry 공식 명세 준수] Kafka (No ZooKeeper)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-kafka-service-0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  volumeName: msa-kafka-pv
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-kafka
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kafka
  template:
    metadata:
      labels:
        app: kafka
    spec:
      # [해결 1] 권한 문제 해결을 위한 초기화 컨테이너 추가
      initContainers:
        - name: fix-permissions
          image: busybox
          command: ["sh", "-c", "chown -R 1001:1001 /var/lib/kafka/data && chmod -R 777 /var/lib/kafka/data"]
          volumeMounts:
            - name: storage
              mountPath: /var/lib/kafka/data
      containers:
        - name: kafka
          image: {{ .Values.kafka.image }}
          env:
            - name: KAFKA_NODE_ID
              value: "1"
            - name: KAFKA_PROCESS_ROLES
              value: "broker,controller"
            - name: KAFKA_LISTENERS
              value: "PLAINTEXT://:9092,CONTROLLER://:9093"
            - name: KAFKA_ADVERTISED_LISTENERS
              value: "PLAINTEXT://kafka-service:9092" # [해결 2] DNS 이름 일치 (kafka-service)
            - name: KAFKA_CONTROLLER_QUORUM_VOTERS
              value: "1@kafka-service:9093"
            - name: KAFKA_CONTROLLER_LISTENER_NAMES
              value: "CONTROLLER"
            - name: KAFKA_LOG_DIRS
              value: "/var/lib/kafka/data"
            - name: KAFKA_INTER_BROKER_LISTENER_NAME
              value: "PLAINTEXT"
          ports:
            - containerPort: 9092
            - containerPort: 9093
          volumeMounts:
            - name: storage
              mountPath: /var/lib/kafka/data
      volumes:
        - name: storage
          persistentVolumeClaim:
            claimName: data-kafka-service-0
---
# [공식 명세] 반드시 kafka-service 라는 이름으로 생성되어야 함
apiVersion: v1
kind: Service
metadata:
  name: kafka-service
spec:
  ports:
    - port: 9092
      name: client
      targetPort: 9092
    - port: 9093
      name: controller
      targetPort: 9093
  selector:
    app: kafka
