server:
  port: 8081 # Agent 포트 (Server와 충돌 방지)

spring:
  application:
    name: cnapp-agent

agent:
  sender:
    type: http # http 또는 kafka
  snapshot:
    schedule:
      rate:
        ms: 60000 # 60초마다 수집

cnapp:
  server:
    url: http://localhost:8080/api/v1/ingestion/raw
